// Production stub — server/vite.ts is only imported when DISABLE_FRONTEND is not set.
// In Replit deployment DISABLE_FRONTEND=1 so this file is never executed at runtime.
import { defineConfig } from "vite";
export default defineConfig({});
