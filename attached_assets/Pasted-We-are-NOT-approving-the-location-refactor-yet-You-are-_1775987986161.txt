We are NOT approving the location refactor yet. You are very close, but there are critical fixes required before this is production-safe.

Apply the following EXACT changes.

---

# 🔧 TASK 1.1 — FINALIZE LOCATION ARCHITECTURE

## 1. ❌ REMOVE ALL REMAINING GPS WATCHERS

Specifically:

* DELETE `navigator.geolocation.watchPosition` from:
  → client/src/pages/home.tsx

Replace with:

* Subscribe to `location-service` instead

🚨 Rule:
There must be **ZERO direct calls to navigator.geolocation anywhere except location-service.ts**

---

## 2. 🔒 ENFORCE SINGLETON LOCATION SERVICE

Ensure:

* Only ONE instance of location-service exists app-wide
* It cannot be re-instantiated accidentally

If needed:

* Export a single instance (not a class to be constructed multiple times)

Example pattern:

```ts
class LocationService { ... }

export const locationService = new LocationService();
```

---

## 3. 🟡 IMPLEMENT WARM CACHE (CRITICAL UX FIX)

When a component subscribes:

* If a last known valid position exists:
  → Immediately emit it

BUT:

* Mark it as **stale** (e.g. include timestamp)
* UI must be able to distinguish:
  → “Last known” vs “Live”

---

## 4. 🚫 BLOCK INVALID FIRST FIX (NO MORE JUMP)

CHANGE behavior:

❌ Current:

* First GPS fix always accepted

✅ Required:

* DO NOT emit position until:
  → accuracy < 100m (or reasonable threshold)

Until then:
→ return NULL state (no position)

---

## 5. 🧭 ADD “LOCATING” STATE SUPPORT

location-service must expose:

* isLocating: boolean

Logic:

* TRUE until first valid GPS fix
* FALSE after valid fix

---

## 6. 🛡 ADD BASIC JITTER FILTER

Reject position if:

* Distance from last point > 5km
  AND
* Time difference < 10 seconds

(This prevents GPS spikes/teleporting)

---

## 7. 📦 OUTPUT REQUIRED

After completion, explain:

1. Confirmation that ALL direct GPS calls are removed except service
2. How singleton is enforced
3. How warm cache works (with example)
4. How “locating” state is exposed
5. What accuracy threshold you used and why

---

# 🚨 DO NOT:

* Do NOT add new features
* Do NOT touch backend
* Do NOT implement heartbeat yet

This is still foundation work.

---

Expected result:

* No teleporting
* No incorrect first pin
* No duplicate GPS watchers
* Smooth initial load with safe UX

---
