Task 3 is approved with final adjustments:

* Update zoom clamp: min 14 (not 12)
* Safety override should only trigger on `concern` state (not all safety events)

---

# 🔧 TASK 4 — NATIVE ACCURACY TRACK (PHASE 1)

## 🎯 OBJECTIVE

Build the **native tracking foundation** for StillHere.

This is NOT full Life360 replication yet.

This phase is about:
→ reliable background location
→ App Store-ready native shell

---

# 🚨 PHASE 1 — BACKGROUND NATIVE TRACKING

## Requirements:

### 1. Capacitor iOS setup

Ensure project supports:

* iOS build via Capacitor
* native plugin integration

---

### 2. Background location capability

Enable:

* Always location permission
* background location updates

iOS requirements:

* proper Info.plist keys:

  * NSLocationAlwaysAndWhenInUseUsageDescription
  * NSLocationWhenInUseUsageDescription
* background modes:

  * location updates

---

### 3. Native tracking integration

Use:

* existing Capacitor background geolocation plugin already referenced
  OR upgrade to proper native tracking layer if needed

Requirements:

* continuous tracking when app in background
* minimal battery impact
* stable updates (no PWA dropouts)

---

### 4. Replace browser dependency

For native build:

* DO NOT rely on navigator.geolocation
* native plugin becomes primary location source

BUT:

* keep location-service architecture intact
* native source feeds into same pipeline

---

### 5. Heartbeat integration

Native tracking must:

* continue sending heartbeat every 60s
* use same API: POST /api/heartbeat

NO CHANGES to server logic.

---

# ⚠️ DO NOT BUILD YET

* trip history
* dwell detection
* route replay
* driving behavior detection
* crash detection refinement

These are Phase 2+ tasks.

---

# 📦 OUTPUT REQUIRED

After completion, report:

1. How native location replaces browser location
2. What plugin / method is used
3. How background tracking behaves (screen off, app closed)
4. How heartbeat integrates with native tracking
5. Any limitations discovered on iOS

---

# ✅ EXPECTED RESULT

After Task 4 Phase 1:

* StillHere runs as native iOS app
* location tracking continues in background
* heartbeat remains consistent
* accuracy and reliability significantly improved

---
