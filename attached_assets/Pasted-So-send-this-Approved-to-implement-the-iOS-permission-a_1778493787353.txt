So send this:

  Approved to implement the iOS permission and privacy wording pass with the following conditions.

  Proceed with:

  1. Replace the risky iOS permission strings in capacitor.config.json with the safer approved wording.

  2. Delete deprecated NSLocationAlwaysUsageDescription.

  3. Change BackgroundGeolocation.locationAuthorizationRequest from "Always" to "WhenInUse".

  Important:
  Do not reduce background-location functionality long term.

  If escalation to Always is not wired in this pass, add a clear TODO in STORE_SUBMISSION.md and code comments stating:

  Safe Walk, Safety Timer, Drive Safety, active sharing, and SOS may require Always Location permission for reliable background
  use.
  The app should request/escalate to Always permission at the moment the user enables one of those features.

  4. Replace the Watch permission strings with safer wording.

  5. Fix Watch bundle ID and companion bundle ID:

  Watch app:
  com.daudabangoura.stillhere.app.watchkitapp

  Companion app:
  com.daudabangoura.stillhere.app

  6. Fix App Group references:

  group.com.daudabangoura.stillhere.app

  7. Gate sendVoipPush behind:

  ENABLE_VOIP_PUSH === "true"

  Do not enable VoIP background mode for launch.

  8. Document VoIP wake-up as Phase 2 in STORE_SUBMISSION.md.

  9. Add manual Xcode capability checklist to STORE_SUBMISSION.md:

  Push Notifications: on
  Background Modes: Location updates, Background fetch, Remote notifications
  Do NOT turn on Voice over IP for launch
  In-App Purchase: on
  HealthKit: Watch target only
  App Groups: phone + Watch targets
  Sign in with Apple: skip
  Associated Domains: skip

  10. Do not add Contacts, Camera, Bluetooth, Speech, ATT, or Android permissions.

  After implementation, report:

  files changed
  exact permission strings changed
  bundle ID fixes
  App Group fixes
  VoIP gate confirmation
  TypeScript result if relevant
  any remaining App Store submission risks