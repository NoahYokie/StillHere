We are NOT approving Task 1 yet. One final architectural issue must be resolved.

---

# 🔧 TASK 1.2 — UNIFY DRIVING MONITOR INTO LOCATION SERVICE

## 🎯 OBJECTIVE

Eliminate the last remaining independent GPS system.

After this task:
→ There must be ONLY ONE system in the entire app that accesses navigator.geolocation

---

## 1. 🚫 REMOVE ALL GPS CALLS FROM driving-monitor.ts

Delete:

* ALL navigator.geolocation usage
* Any internal watchPosition or getCurrentPosition logic

---

## 2. 🧠 UPGRADE location-service TO SUPPORT MODES

Add support for:

### Modes:

* "normal" (default)
* "high_accuracy" (for driving / SOS)

---

## 3. ⚙️ IMPLEMENT MODE CONTROL

Add methods:

```ts
startHighAccuracyMode()
stopHighAccuracyMode()
```

Behavior:

* When ANY subscriber requests high accuracy:
  → GPS config upgrades:

  * enableHighAccuracy: true
  * lower maximumAge
  * faster updates

* When no high-accuracy subscribers remain:
  → revert to normal mode

---

## 4. 🔄 SINGLE GPS STREAM WITH ADAPTIVE CONFIG

The GPS watcher must:

* Dynamically adjust behavior based on active mode
* NOT create a second watchPosition

---

## 5. 🔌 CONNECT DRIVING MONITOR

Update driving-monitor.ts:

* Subscribe to location-service

* Use location data from service ONLY

* When driving starts:
  → call startHighAccuracyMode()

* When driving ends:
  → call stopHighAccuracyMode()

---

## 6. ⚠️ IMPORTANT RULE

driving-monitor.ts:

* MUST NEVER act as a location source
* MUST NEVER write coordinates to UI or override location-service

It is:
→ analysis only (speed, crash detection)

---

## 7. 📦 OUTPUT REQUIRED

Explain:

1. Confirmation that ALL GPS calls are removed from driving-monitor.ts
2. How mode switching works internally
3. How conflicts are handled if multiple features request high accuracy
4. Whether watchPosition is restarted or reused when switching modes

---

# 🚨 DO NOT:

* Do NOT add new features
* Do NOT modify UI
* Do NOT implement Safety State Engine yet

---

# ✅ EXPECTED RESULT

* TRUE single source of location
* Adaptive accuracy (driving = precise, normal = efficient)
* No architectural exceptions
* Ready for Safety State Engine

---
