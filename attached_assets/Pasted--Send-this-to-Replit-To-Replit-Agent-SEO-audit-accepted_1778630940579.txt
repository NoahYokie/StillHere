 Send this to Replit:

  To Replit Agent:

  SEO audit accepted. Proceed with implementation, scoped as below.

  Principle:
  StillHere should sound premium, clear, and trustworthy. Do not make the copy generic or weak. Do not add SEO keyword stuffing. Do
  not reintroduce any safety guarantees.

  Approved SEO fixes for this batch:

  1. client/index.html metadata

  Title:
  StillHere — Safety Check-In App with SOS, GPS, and Safe Walk

  Meta description:
  Personal safety check-ins for families, seniors, lone workers, and people living alone. SOS, live GPS, Safe Walk, fall sensing,
  and clear next steps.

  Use this same safe direction for OG/Twitter, but keep it polished.

  Approved OG/Twitter:

  og:title:
  StillHere — Personal Safety Check-Ins, SOS, and GPS

  og:description:
  When you can't check in, StillHere checks on you. Daily check-ins, SOS alerts, fall sensing, and live GPS for the people you
  love.

  twitter:title:
  StillHere — Safety Check-Ins, SOS, and Live GPS

  twitter:description:
  When you can't check in, StillHere checks on you. Built for families, seniors, lone workers, and people living alone.

  2. Remove meta keywords entirely.
  Reason: no SEO value, and current keywords include risky/off-brand terms.

  3. JSON-LD cleanup

  Replace the current JSON-LD with safe structured data:
  - Organization
  - WebSite
  - MobileApplication

  Remove aggregateRating entirely. Do not fake ratings or reviews.

  For MobileApplication:
  - name: StillHere
  - operatingSystem: iOS, Android, Web
  - applicationCategory: HealthApplication
  - description: Personal safety check-in app with SOS alerts, fall sensing, live GPS, and a Safety Circle that gets clear next
  steps. StillHere is not an emergency response service.
  - offers: include monthly and yearly only if you can confirm the real current prices from the repo billing config. If not
  confirmed, omit offers for now rather than shipping wrong prices.

  4. Fonts

  Do the grep first, then slim the Google Fonts import aggressively.
  Goal: keep only the fonts actually used by Tailwind/theme/app surfaces.
  Do not blindly remove a font if it is clearly referenced, but the current 27-family import should be reduced heavily.

  5. Social image

  Add a proper 1200x630 OG image path and metadata:
  - og:image
  - og:image:width 1200
  - og:image:height 630
  - og:image:alt
  - twitter:image
  - twitter:image:alt

  Use an existing StillHere branded asset if one exists. If no proper 1200x630 asset exists, create a simple premium static OG card
  from existing brand assets only:
  - StillHere logo/name
  - short line: “Safety check-ins, SOS, and GPS for your Safety Circle”
  - clean background
  - no emergency guarantees
  - no medical claims
  - no fake phone screenshots unless the asset already exists

  Do not use AI-generated people or stock-looking emergency imagery.

  6. robots.txt and sitemap.xml

  Add:
  - robots.txt
  - sitemap.xml

  robots.txt should:
  - allow normal public pages
  - point to sitemap.xml
  - disallow sensitive/token/private routes:
    /emergency/
    /e/
    /setup/
    /settings
    /watched
    /inbox
    /chat
    /call
    /report
    /weekly-report
    /live-location
    /safety-circle
    /safety-timer
    /safe-walk
    /saved-places
    /family
    /billing

  Sitemap should include only public marketing/legal/help pages:
  /
   /help
   /trust
   /privacy
   /terms
   /limitations
   /tour

  Only include routes that actually exist.

  7. Noindex for private/token routes

  Add runtime noindex/nofollow handling for sensitive SPA routes if there is a clean existing pattern.
  Priority routes:
  - /emergency/*
  - /e/*
  - authenticated/private app routes

  If this needs a new head manager or route wrapper, keep it lightweight. Do not add a heavy dependency unless necessary.

  8. Viewport accessibility

  Remove:
  maximum-scale=1
  user-scalable=no

  Keep normal responsive viewport behavior and viewport-fit=cover.
  Reason: pinch zoom should work.

  9. Per-route titles/descriptions

  Add a lightweight route metadata solution for public pages only:
  - /
  - /help
  - /trust
  - /privacy
  - /terms
  - /limitations
  - /tour

  Do not rely on this for OG tags. Static index.html remains the main social source.

  10. H1 decision

  Use this H1 structure if it fits the design without making the hero feel clunky:

  Personal safety check-ins.
  When you can't check in, StillHere checks on you.

  If it makes the hero visually worse, keep the current H1 and add the SEO phrase nearby in visible supporting copy. Do not make
  the hero average just to satisfy a scanner.

  11. FAQPage JSON-LD

  Defer FAQPage JSON-LD for now.
  Reason: we should audit the FAQ answers as a unit before turning them into structured data. Do not add it in this batch.

  12. Service worker

  Quickly inspect sw.js caching behavior. If it aggressively caches index.html or metadata, flag it. Do not make a risky service-
  worker rewrite in this batch unless the issue is obvious and tiny.

  Run after implementation:
  - npx tsc --noEmit
  - npm run build
  - verify robots.txt and sitemap.xml are in the built output
  - grep for removed risky/off-brand SEO terms:
    dementia GPS tracker
    dead mans switch
    panic button app
    knows what to do
    aggregateRating
    ratingCount
    meta name="keywords"

  Report:
  - files changed
  - exact title/description/OG/Twitter final values
  - JSON-LD final shape
  - fonts removed/kept and why
  - robots/sitemap contents
  - noindex routes handled
  - tsc/build result
  - any SEO items intentionally deferred