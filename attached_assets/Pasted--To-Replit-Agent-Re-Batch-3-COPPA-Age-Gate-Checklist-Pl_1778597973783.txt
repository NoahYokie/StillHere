 To: Replit Agent

  Re: Batch 3 COPPA / Age Gate Checklist

  Plan is approved in principle. Answers to Q1-Q5 below.

  Q1. New-user age gate implementation

  Use option B.

  Thread ageConfirmed into verifyOtp and do not create the user row unless ageConfirmed === true for a new user.

  Reason:
  Cleaner audit trail.
  No temporary under-13 user row.
  No cleanup race.
  No orphan account.

  If returning user already exists, do not require ageConfirmed for login.

  Q2. Existing users with NULL ageGateAcceptedAt

  Do not block existing users in Batch 3.

  No one-time modal for existing users in this batch.

  Reason:
  This batch is about v1 signup gating and under-13 lockout, not disrupting existing accounts.

  But add a TODO in STORE_SUBMISSION.md:
  Existing users with NULL ageGateAcceptedAt may be prompted in a future compliance pass if needed.

  Q3. Legacy child/teen role label

  Use:

  Member

  for existing child/teen rows.

  Do not use Family member unless the UI needs more clarity. Keep it short.

  Q4. Server-side rejection scope

  Apply the child/teen role rejection to every route or storage method that can create or update family_members.role.

  Do not limit it only to the two known routes.

  During coding, grep for every write path that touches family_members.role or invite role.

  At minimum:

  POST /api/family/invite
  PATCH /api/family/member/:memberId
  storage.inviteFamilyMember
  storage.updateFamilyMember

  If any other route can write role, block child/teen there too.

  Existing rows can still be read and displayed as Member.

  Q5. App Store Connect age-rating answers

  Yes, draft the App Store Connect age-rating answers in STORE_SUBMISSION.md as documentation only.

  Use:

  Made for Kids: No
  Primarily directed to children under 13: No
  Age rating: 12+ recommended
  StillHere is intended for users 13 and older

  Do not submit anything to App Store Connect from code.

  Other requirements:

  1. Signup UI

  Add checkbox:

  I confirm I am 13 or older.

  If not checked, the user cannot continue.

  If server returns age_gate_required, show:

  StillHere is not available for users under 13.

  Do NOT say:
  Please ask an adult to set up an account on your behalf.

  2. Family Mode

  Hide Teen and Child from role picker.

  Existing child/teen rows show as Member.

  Server rejects child/teen role creation or update.

  Remove or rephrase any copy that suggests under-16 or child tracking, including:

  “Under-16 members need a guardian's permission first.”

  3. Terms and Privacy

  Add exact wording:

  StillHere is intended for users 13 and older. We do not knowingly collect personal information from children under 13. If we
  learn we have, we will delete the account and associated data.

  4. Tests

  Approved test list T1-T9.

  No jsdom/render test required in this batch. Manual visual check for the Family role label is enough.

  5. No scope creep

  Do not add DOB.
  Do not build parental consent.
  Do not remove enum values from database.
  Do not delete existing child/teen rows.
  Do not start Batch 4.

  Proceed with Batch 3 only.

  After implementation, report:

  files changed
  schema changes
  db:push result
  test results
  signup behavior
  family role lockout behavior
  privacy/terms copy
  App Store age-rating documentation
  confirmation no existing family rows were deleted
  remaining risks

  Simple explanation:

  We are choosing the safest simple route:

  - StillHere is 13+
  - no date of birth stored
  - new users must confirm they are 13+
  - under-13 signup blocked
  - no child/teen family roles shown
  - server blocks child/teen roles
  - old child/teen rows do not break, they just display as “Member”

  This avoids building a complicated parental consent system before launch.