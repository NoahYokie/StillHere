To: Replit Agent

  Re: Final Checklist Review — Agreement With Two Safety Refinements, No Build Yet

  The checklist is strong and mostly approved in principle.

  I agree with:

  Group 1 copy edits
  Group 2 Limitations screen
  acknowledgedLimitationsAt schema column
  two-step implementation
  no feature removal
  no behavior reduction
  no app-wide Guardian refactor
  "SOS from {name}"
  SEO/meta wording changes
  no em dash rule

  But before founder approval, I need two refinements locked.

  Refinement 1 — Limitations screen must never block urgent safety use

  The line:

  “Existing users will see the screen on their next app open since their acknowledgedLimitationsAt will be null.”

  is legally reasonable, but it creates a safety risk if implemented too aggressively.

  Do NOT hard-block urgent flows behind the Limitations screen.

  The Limitations screen must not block:

  SOS
  discreet SOS
  active incident view
  missed check-in recovery flow
  Safe Walk active flow
  Safety Timer active/grace/escalated flow
  Drive Safety active/crash countdown flow
  guardian/watcher emergency token pages
  incoming Safety Circle alert pages
  any route used by contacts to respond during an incident

  For new users:
  It is fine to show the Limitations screen before setup is considered complete.

  For existing users:
  Prompt them once on next normal app open, but do not trap them away from emergency actions.

  Preferred approach:

  If acknowledgedLimitationsAt is null and there is no active safety event:
  show /limitations before normal dashboard use.

  If there is an active safety event or urgent route:
  allow the urgent route first, then show the Limitations screen after the event is resolved or on the next calm app open.

  On the Limitations screen itself, include a small safety escape:

  “If you need help right now, use SOS instead.”

  This should route to the existing SOS flow without marking the limitations as acknowledged.

  Do not make this escape visually dominant, but it must exist.

  Reason:
  A legal disclaimer should not prevent someone from using the app during a real safety moment.

  Refinement 2 — Group 2 should be implemented after Group 1 review

  Confirm execution order:

  First implement Group 1 only.
  Return diff and verification.
  Wait for review.

  Then implement Group 2.
  Return diff and verification.

  Do not combine Group 1 and Group 2 in one build pass.

  Additional small copy lock:

  For the Limitations button, use:

  I understand. Continue

  No em dash.

  For the Trust page link, label:

  What StillHere is, and what it isn't

  If opened from Trust page after acknowledgement, it should be read-only / re-read mode and should not trap the user. Button can
  be:

  Back

  or

  Done

  Please return a final revised checklist with these two refinements included.

  No code changes yet.

  Simple explanation:

  Replit’s plan is good, but one thing needs fixing before approval.

  They want existing users to see the Limitations screen next time they open the app. That is good legally.

  But it must not block emergency use.

  Example: if someone opens StillHere because they need SOS, they should not be forced to scroll through a legal screen first.

  So the rule should be:

  - New users see the Limitations screen during setup.
  - Existing users see it once during normal use.
  - If there is an active safety event, SOS, Safe Walk, Safety Timer, Drive Safety alert, missed check-in, or contact emergency
    page, the app lets them handle that first.
  - Then it shows the Limitations screen later.
  - The Limitations screen should include a small escape: “If you need help right now, use SOS instead.”

  That protects the company without reducing StillHere’s safety features.