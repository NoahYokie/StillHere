To Replit Agent:

  Good. This is the level of detail I needed. I agree with most of your definitions and have answers to Q1 and Q2.

  Q1: Drive Safety stale sessions

  Choose option (a): add a 24-hour staleness cutoff to getActiveDriveSession.

  Do not add an expiresAt column in this fix. That is cleaner long term, but larger scope.

  Policy for this fix:

  Drive Safety active =
  userId matches
  AND endedAt IS NULL
  AND startedAt >= NOW() - 24 hours

  If a drive session is older than 24 hours and still not ended, it must not keep nativeTrackingAllowed=true.

  Reason:
  We need to close the privacy hole without redesigning drive sessions. A stale drive from yesterday should not keep background GPS
  alive forever.

  Please also log a PII-free warning when an unended stale drive session is ignored by the tracking policy, for example:
  [TRACKING_POLICY] ignoring stale drive session for userId reason=drive_stale_24h

  Do not log coordinates.

  Q2: Test incidents / Safety Circle Dry Run

  Confirmed: test incidents must be excluded from incident active tracking.

  A dry run should not enable native background GPS.

  Policy:

  Incident active for native tracking =
  status === "open"
  AND reason IN ("missed_checkin", "sos")

  Exclude:
  reason === "test"
  status === "paused"
  status === "resolved"

  Reason:
  A Safety Circle Dry Run should test alert flow without turning on real background tracking.

  Other definitions accepted:

  Active shift:
  location_sessions.type = "shift"
  AND active = true
  AND expiresAt > now

  Emergency location session:
  location_sessions.type = "emergency"
  AND active = true
  AND expiresAt > now

  Safe Walk active:
  status IN ("active", "overdue", "escalated")

  Safety Timer active:
  status IN ("active", "grace_period", "escalated")
  For status "active", expiresAt > now is required.
  For grace_period and escalated, do not require expiresAt > now because those are post-expiry safety states.

  Incident active:
  status = "open"
  AND reason IN ("missed_checkin", "sos")

  Drive Safety active:
  endedAt IS NULL
  AND startedAt >= now minus 24 hours

  Location mode rules:

  off:
  nativeTrackingAllowed=false

  emergency_only:
  allow only if a real active safety purpose exists:
  - liveShare
  - safeWalk
  - driveSafety
  - safetyTimer
  - incident
  - emergency location session

  on_shift_only:
  allow only if activeShift=true

  both:
  allow ongoing GPS if sharingMode is not paused

  sharingMode rules:

  paused:
  nativeTrackingAllowed=false always

  presence:
  Question before build: confirm whether presence should allow native collection but hide coordinates from watchers, or whether
  presence should disable native GPS. My instinct:
  - presence should NOT require native GPS unless another active safety purpose exists
  - precise and area can allow GPS when policy says tracking is allowed
  - paused always stops GPS

  Please confirm current intended meaning of presence before build.

  Heartbeat:
  Accepted.
  Client must avoid attaching lat/lng/acc when nativeTrackingAllowed=false.
  Server must strip lat/lng/acc when nativeTrackingAllowed=false.

  Client chokepoint:
  Accepted.
  Put assertNativeTrackingAllowed() inside startLiveTracking() so all callers inherit it.

  Mid-session policy:
  Accepted.
  Use all three:
  - immediate client reaction to settings mutation response
  - socket tracking-policy:changed
  - 60-second polling while native tracking is active

  Grace rule:
  Accepted.
  - 5 minutes max
  - 3 failed polls max
  - 401/403 fail closed immediately
  - no cached approval fail closed
  - stale cached approval fail closed
  - repeated failures do not extend the window forever

  Central policy helper:
  Accepted.
  Create server/tracking-policy.ts with getTrackingPolicyForUser(userId).

  Before I give final build authorization, answer this one final question:

  What is the current intended product meaning of sharingMode = "presence"?

  Specifically:
  A. Does presence mean "collect GPS but only show contacts present/away style state"?
  B. Or does presence mean "do not collect precise GPS unless a specific active safety session requires it"?

  My recommendation is B for privacy:
  presence should not keep native GPS running by itself.
  presence can still allow non-location heartbeat.
  If a real safety purpose starts, GPS can run because the active purpose authorizes it.

  Please confirm whether B would break any existing StillHere feature.

  After that, I can give final build authorization.

  Simple explanation for you:

  We solved the drive issue:

  - A drive session should not keep GPS on forever.
  - If a drive is older than 24 hours and never ended, ignore it for background tracking.

  We solved the test alert issue:

  - A Safety Circle test should not turn on real GPS tracking.
  - Only real missed check ins or SOS incidents should count.

  One final detail remains: presence mode.

  We need to know what it means.

  If presence means:

  > “Just show that I’m okay / active, but don’t show exact location”

  then it should probably not keep GPS running by itself.

  If presence currently depends on GPS to know if the user is moving or safe, then Replit needs to explain that before we approve
  the build.

  My recommendation is privacy-first:

  presence = heartbeat only, no native GPS unless a real safety session is active

  That protects the brand promise: safety without surveillance.