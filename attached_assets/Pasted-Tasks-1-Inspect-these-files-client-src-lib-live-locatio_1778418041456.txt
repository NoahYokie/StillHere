Tasks:

  1. Inspect these files:
  - client/src/lib/live-location.ts
  - client/src/lib/location-service.ts
  - client/src/App.tsx
  - client/src/main.tsx
  - any Capacitor/background geolocation config
  - server routes used by /api/live-location/status

  2. Add a first-frame location privacy guard.

  On app startup, before any background tracking continues, call the server status endpoint.

  If the server says:
  - no active live location session
  - no active Safe Walk
  - no active Drive Safety
  - no active SOS/emergency location session
  - sharingMode is paused
  - locationMode is off
  - request returns 401 or 403

  Then force stop native background location immediately.

  The stop must call the actual BackgroundGeolocation stop/remove listeners method, not only update React state.

  3. Add a guard before starting tracking.

  Before any user action calls startLiveTracking() or startWatch(), verify with the server that tracking is allowed for the
  requested feature.

  Do not allow native BackgroundGeolocation.start() unless the backend confirms an active/allowed session.

  4. Server-side status endpoint must be the source of truth.

  Confirm /api/live-location/status returns enough information for the client to decide:
  - active: true/false
  - sharingMode
  - locationMode
  - active session type if available
  - whether native tracking is allowed

  If missing, add a safe field like:
  nativeTrackingAllowed: true/false

  5. Protect against stale sessions.

  If the server finds an expired or cancelled live location / Safe Walk / Drive / SOS location session, it should return
  nativeTrackingAllowed:false.

  6. Add logs without leaking location data.

  Log:
  - when startup guard stops background location
  - when tracking start is blocked
  - reason code only

  Do not log lat/lng.

  7. Tests or manual verification required.

  Verify these cases:
  - locationMode off → app startup stops native background location
  - sharingMode paused → app startup stops native background location
  - no active session → app startup stops native background location
  - 401/403 from status endpoint → app startup stops native background location
  - active live location session → native tracking allowed
  - active Safe Walk session → native tracking allowed
  - active SOS/emergency location session → native tracking allowed
  - tapping Start Live Location while not allowed does not start native plugin
  - tapping Start Live Location after server creates an allowed session does start native plugin
  - no lat/lng appears in logs

  8. Report back:
  - files changed
  - exact code paths changed
  - status endpoint response shape
  - verification results
  - whether startOnBoot remains enabled or was changed
  - any remaining privacy risk

  Do not change unrelated app behavior.

  This is the one that protects you from the biggest privacy claim risk: background location continuing when the user did not mean
  it to.