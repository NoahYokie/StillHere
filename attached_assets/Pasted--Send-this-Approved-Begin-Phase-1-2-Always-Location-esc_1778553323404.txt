 Send this:

  Approved. Begin Phase 1.2 Always Location escalation UX.

  Build exactly from the approved plan.

  Requirements:

  1. Do not reduce StillHere safety features.

  2. Do not block:

  SOS
  missed check-in alerts
  Safety Timer start
  emergency incident flows

  3. Implement:

  BackgroundLocationProvider
  useBackgroundLocationEscalation
  BackgroundLocationSheet
  BackgroundLocationWarningBanner

  4. Fix Drive Safety page leak.

  Opening the Drive page must not start native GPS.

  Native GPS should start only after the user taps Start and the Always permission check passes.

  5. Add tracking-policy gate inside LocationService.startWatch.

  If nativeTrackingAllowed=false, do not start native BackgroundGeolocation.

  6. Implement per-feature behavior:

  Safe Walk:
  Allow with persistent warning if only When In Use.

  Safety Timer:
  Never block. Show last-known-location warning if not Always.

  Drive Safety:
  Require Always. Block start if not granted.

  Precise sharing:
  Require Always. Revert toggle if not granted.

  Area sharing:
  Allow with persistent warning if only When In Use.

  Presence:
  No prompt.

  Paused:
  No prompt.

  SOS:
  Never block.

  Missed check-in:
  Never block.

  7. Add settings dashboard Upgrade to Always button.

  8. Update STORE_SUBMISSION.md Section 5c with:

  per-feature behavior table
  Drive Safety blocked-card behavior
  Precise sharing blocked behavior
  Safe Walk / Area degraded banner behavior
  SOS and missed-check-in never-block rule

  9. Add tests as planned.

  10. After implementation, return:

  files changed
  feature-by-feature behavior confirmation
  Drive Safety leak fix confirmation
  tracking-policy gate confirmation
  test results
  TypeScript result
  confirmation SOS and missed check-in are not blocked
  remaining risks

  Stop after Phase 1.2 and wait for review.

  This is the right next move.