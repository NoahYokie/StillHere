To: Replit Agent

  Audit Fix #3 is approved for implementation.

  Build the locked tracking policy exactly as agreed.

  Primary goal:
  Close the background location privacy gap without reducing StillHere’s core safety features.

  Implement:

  1. Server source of truth

  Create:

  server/tracking-policy.ts

  Export:

  getTrackingPolicyForUser(userId)

  This must be the single source of truth for:

  nativeTrackingAllowed
  heartbeatAllowed
  sharingMode
  locationMode
  activePurposes
  sessions
  graceWindowSeconds
  reason

  2. Replace /api/live-location/status

  Return the full policy response:

  nativeTrackingAllowed
  heartbeatAllowed
  sharingMode
  locationMode
  activePurposes[]
  sessions{}
  graceWindowSeconds: 300
  reason

  3. Heartbeat enforcement

  In POST /api/heartbeat:

  Compute tracking policy server side.

  If nativeTrackingAllowed=false:

  drop lat
  drop lng
  drop acc

  Continue processing non-location heartbeat fields.

  Log only:

  [TRACKING_POLICY] heartbeat location stripped userId=... reason=...

  No coordinates in logs.

  4. Settings responses

  For:

  POST /api/settings
  POST /api/settings/pause
  any sharingMode mutation route

  Return:

  nativeTrackingAllowed
  heartbeatAllowed
  reason

  If policy flips to false, the client must stop native GPS immediately.

  5. Socket event

  Emit:

  tracking-policy:changed

  when any of these change:

  settings change
  sharingMode change
  locationMode change
  incident open
  incident resolve
  Safe Walk transition
  Safety Timer transition
  Drive Safety start
  Drive Safety end
  shift start
  shift end
  emergency location session start
  emergency location session end

  6. Storage helpers and active definitions

  Add or update helpers as needed.

  Active shift:

  location_sessions.type='shift'
  active=true
  expiresAt > now

  Active emergency session:

  location_sessions.type='emergency'
  active=true
  expiresAt > now

  Safe Walk active:

  status IN ('active','overdue','escalated')

  Safety Timer active:

  status='active' AND expiresAt > now

  OR

  status IN ('grace_period','escalated')

  Incident active:

  status='open'
  reason IN ('missed_checkin','sos')

  Exclude test incidents.

  Drive Safety active:

  endedAt IS NULL
  startedAt >= now - 24 hours

  If a stale drive session is ignored, log:

  [TRACKING_POLICY] ignoring stale drive session userId=... reason=drive_stale_24h

  No coordinates in logs.

  7. Final policy matrix

  nativeTrackingAllowed=true only if:

  user authenticated
  sharingMode != 'paused'
  locationMode != 'off'

  AND one approved purpose is true:

  active liveShare
  active safeWalk
  active driveSafety
  active safetyTimer
  open real incident
  active emergency session
  active shift
  locationMode='both' with ongoing consent, but not presence or paused

  For locationMode='on_shift_only':

  GPS allowed only during active shift or another active safety purpose.

  For locationMode='emergency_only':

  GPS allowed only during real safety purpose or emergency session.

  For sharingMode='presence':

  Do NOT allow ongoing native GPS by itself.

  Presence mode behavior:

  nativeTrackingAllowed=false in calm time
  heartbeatAllowed=true
  lat/lng hidden from watchers as before
  concern override preserved
  GPS allowed only when a real active safety purpose begins

  8. One-shot location mitigation

  When policy flips from:

  nativeTrackingAllowed=false

  to:

  nativeTrackingAllowed=true

  because a real safety purpose starts:

  call Geolocation.getCurrentPosition() once immediately
  send that point to the server
  then start BackgroundGeolocation

  This reduces the “Locating…” delay for watchers.

  9. Client guard

  In:

  client/src/lib/live-location.ts

  Add:

  assertNativeTrackingAllowed()

  Call it inside startLiveTracking(), so all existing start paths inherit it.

  Also re-check on:

  visibilitychange
  focus
  60-second poll while native tracking is active
  tracking-policy:changed socket event

  10. Grace rule

  Client may keep tracking briefly during network failure only if it has a recent approved policy.

  Stop native GPS if:

  no cached approval
  cached approval older than 5 minutes
  3 consecutive failed policy polls
  401 or 403 response

  401/403 must stop immediately.

  11. Heartbeat client patch

  In:

  client/src/lib/heartbeat.ts

  Only attach lat/lng/acc if cached nativeTrackingAllowed=true.

  Server strip still remains the authority.

  12. Verification required

  Run and report these cases:

  presence user calm time:
  no native GPS, heartbeat without coordinates

  presence user enters concern:
  policy flips true, one-shot location fires, watcher receives coordinate after short locating state

  paused mid-session:
  socket event received, native GPS stops within 2 seconds

  locationMode off mid-session:
  native GPS stops

  stale drive session older than 24h:
  ignored, no GPS allowed, PII-free log present

  Safety Circle dry run / test incident:
  does not enable native GPS

  network drop:
  continues only until 5 minutes or 3 failed polls, then stops

  401/403:
  immediate stop

  heartbeat spoof attempt with lat/lng while nativeTrackingAllowed=false:
  server strips coordinates

  Safe Walk active, overdue, escalated:
  GPS allowed

  Safety Timer active, grace_period, escalated:
  GPS allowed

  incident missed_checkin or sos:
  GPS allowed

  incident test:
  GPS not allowed

  on_shift_only without active shift:
  GPS not allowed unless another active safety purpose exists

  on_shift_only with active shift:
  GPS allowed

  locationMode both with sharingMode precise or area:
  ongoing GPS allowed

  locationMode both with sharingMode presence:
  no calm-time GPS, only active safety purpose GPS

  13. Deliver back

  After build, report:

  files changed
  important diffs
  verification results
  whether any existing StillHere safety feature was affected
  any residual risks

  Do not change unrelated features.
  Do not weaken SOS, missed checkin, Safe Walk, Safety Timer, Drive Safety, active shift, or watcher concern behavior.