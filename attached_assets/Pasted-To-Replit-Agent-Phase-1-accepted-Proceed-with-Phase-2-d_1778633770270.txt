To Replit Agent:

  Phase 1 accepted.

  Proceed with Phase 2 discussion only first. Do not write code yet.

  Goal:
  Fix the reused-token UX cliff so every incident-time SMS/email link gives the watcher a fresh link with close to the full 24-hour
  lifetime.

  Founder policy:
  No public watcher/emergency token link lasts longer than 24 hours.
  But a newly sent incident alert should not reuse a token that is already 20+ hours old.

  Direction:
  I agree with your lean toward option (b): mint fresh incident-scoped tokens at incident-time instead of reusing standing tokens.

  Please audit and propose the exact Phase 2 plan.

  Questions to answer:
  1. Which exact call sites currently send watcher/contact/emergency links using regenerated or standing tokens?
  2. Which of those should mint purpose="incident" tokens instead?
  3. Which should keep purpose="standing" tokens?
  4. Should purpose="incident" tokens be revoked or resolved-state-gated after incident resolution, or both?
  5. Can every incident-time token be tied to a specific incidentId today, or does contact_tokens lack incidentId? If no incidentId
  column exists, do NOT propose a schema migration unless truly needed.
  6. Are email links and SMS links generated from the same token source or separate flows?
  7. Does force-fresh token minting risk sending multiple valid links for the same contact during escalation? If yes, is that
  acceptable under the 24h cap?
  8. What tests will prove new incident SMS/email links get a fresh 24h lifetime and still expire at 24h max?

  Preferred policy:
  - Incident-time links: fresh token, purpose="incident", TTL 24h max.
  - Standing watcher-card/settings links: can keep reuse behavior if needed.
  - All-clear links: unchanged, purpose="allclear", shorter TTL.
  - After incident resolution: incident/standing alert links should show resolved state and no live location.
  - Revocation on resolution is nice if simple, but resolved-state gating is the minimum requirement. Do not break all-clear links.

  Return:
  - call-site table
  - proposed implementation plan
  - exact token purpose/TTL per link type
  - whether you recommend option (b) or another approach
  - tests
  - risks

  No code until Codex/founder approves.