To Replit Agent:

  Audit accepted. Proceed with implementation, but keep it scoped and staged.

  Policy approved:
  No public watcher, responder, emergency, drill, or report link should last longer than 24 hours.

  Adopt Policy A through F as proposed:
  - Emergency/SOS/contact responder links expire at the earliest of 24 hours, incident resolved, user cancels alert, or manual
  revoke.
  - Missed check-in links same rule.
  - Public live location links do not exist today. If created later, they must be 24h max and tied to session stop/resolution.
  - Safety Circle invite links should default to 24h if tokenized in the future. Today family invite has no token, so no change.
  - Drill/test public links do not exist today. If created later, 24h max and expire on drill completion/cancel.
  - Public report token links do not exist today. Authenticated reports only.

  Approve Phase 1 first. This is the privacy fix.

  Phase 1 implementation:
  1. When an incident is resolved or cancelled, stop the original /emergency/:token standing link from showing live status, last
  check-in location, or live-share location.
  2. Do not break the fresh all-clear /e/:token link.
  3. If a watcher opens the original alert link after resolution, show a resolved-state page instead of live data.

  Approved resolved-state wording:
  This alert has been resolved.
  {Name} confirmed they are safe at {time, local}. No further action is needed.
  For privacy, live status is no longer shown on this link. Open the StillHere app to view current status.

  If the resolved time is not available, use:
  This alert has been resolved.
  For privacy, live status is no longer shown on this link. Open the StillHere app to view current status.

  Approved expired-link wording:
  This safety link has expired.
  For privacy, StillHere links work for up to 24 hours. If you are worried about someone's safety right now, please call them
  directly, or contact your local emergency services.
  StillHere is not an emergency response service.

  Important:
  - Do not revoke or break all-clear links minted after resolution.
  - Do not show location on the resolved page.
  - Do not show live location after incident resolution.
  - Do not weaken active emergency alert pages. Urgent active alerts should still be clear and actionable.
  - No schema migration unless absolutely necessary.

  Phase 2 after Phase 1:
  Fix the reused-token UX cliff by forcing fresh 24-hour tokens for incident-time SMS/email links. Do not start Phase 2 until Phase
  1 is reported and accepted.

  Phase 3 after Phase 2:
  Add link expiry wording to SMS/email:
  SMS trailer:
  Link expires in 24 hours.

  Email trailer:
  This link expires in 24 hours for your safety and privacy.

  Phase 4 after Phase 3:
  Hygiene verification:
  - Confirm /e/:token uses noindex/noarchive/nosnippet protection or the same protected API path.
  - Confirm /report/:userId is RequireAuth. If not, report first before changing.
  - Confirm no public token route is indexable.

  Please implement Phase 1 only now.

  Report:
  - files changed
  - exact behavior before/after
  - how all-clear links are protected
  - tsc result
  - targeted tests/manual verification
  - any reason Phase 2 needs to be adjusted