To: Replit Agent

  Next audit item: iOS permission strings and privacy wording.

  Do not build yet.

  We need to prepare StillHere for App Store review by checking iOS permission strings, background mode justification, HealthKit/
  watch wording, notification wording, contacts wording, microphone/call wording, and privacy nutrition consistency.

  Goal:
  Make permission prompts accurate, specific, and not overclaiming.

  Do not remove or reduce StillHere features.

  Core principle:
  Permission copy should clearly explain why StillHere needs the permission, but must not imply guaranteed rescue, emergency
  services, medical-grade detection, or continuous surveillance.

  Please audit current code/config and return a report first.

  Scope:

  1. iOS Info.plist / Capacitor config

  Check current strings for:

  NSLocationWhenInUseUsageDescription
  NSLocationAlwaysAndWhenInUseUsageDescription
  NSLocationAlwaysUsageDescription
  NSMotionUsageDescription
  NSMicrophoneUsageDescription
  NSCameraUsageDescription, if camera/file picker is used
  NSContactsUsageDescription, if importing contacts is supported
  NSHealthShareUsageDescription, if watch/HealthKit reads heart rate
  NSHealthUpdateUsageDescription, if anything writes to HealthKit
  NSUserTrackingUsageDescription, only if app tracking transparency is used
  Bluetooth permissions, if any wearable or nearby-device feature uses Bluetooth
  Speech recognition, if any voice transcription exists

  Also check:

  UIBackgroundModes
  location
  fetch
  remote-notification
  voip, only if truly used
  audio, only if truly used

  Push Notifications capability
  Background Modes capability
  HealthKit capability
  WatchConnectivity capability
  Associated Domains, if used
  Keychain sharing / App Groups, if watch companion uses shared access

  2. Android manifest status

  Check whether Android exists yet.

  If android/ does not exist, report that clearly.

  If it exists, audit:

  ACCESS_FINE_LOCATION
  ACCESS_COARSE_LOCATION
  ACCESS_BACKGROUND_LOCATION
  POST_NOTIFICATIONS
  RECEIVE_BOOT_COMPLETED
  FOREGROUND_SERVICE
  FOREGROUND_SERVICE_LOCATION
  ACTIVITY_RECOGNITION
  RECORD_AUDIO
  READ_CONTACTS
  CAMERA
  BODY_SENSORS
  INTERNET
  BILLING

  Do not add Android yet unless founder later approves.

  3. Permission wording style

  Use wording like:

  “StillHere uses your location to share it with your Safety Circle when you choose, or during safety alerts such as SOS, Safe
  Walk, Drive Safety, or missed check-ins.”

  Avoid:

  “StillHere tracks you”
  “StillHere monitors you”
  “StillHere protects you”
  “StillHere detects emergencies”
  “StillHere guarantees alerts”

  4. Feature truth mapping

  For each permission, map:

  permission
  why StillHere uses it
  feature requiring it
  whether it is always needed or only optional
  risk if wording is inaccurate
  recommended string

  5. Background location justification

  This is critical.

  We need a strong explanation for Apple:

  StillHere needs background location for optional safety features such as:

  SOS location sharing
  Safe Walk
  Drive Safety
  active location sharing
  missed check-in incident location
  active shift / on-shift safety mode
  Safety Timer escalation, if location is used there

  But we must also say:

  Location sharing is off by default.
  Users can choose location modes.
  Presence mode does not collect GPS during calm time.
  Background location is used only when enabled or when a safety flow requires it.
  StillHere is not a continuous tracking app by default.

  6. Health / fall / crash wording

  If HealthKit or watch heart rate is used:

  Do not say:

  medical monitoring
  diagnosis
  healthcare
  detects health emergency
  guaranteed fall detection

  Use:

  heart-rate readings
  possible fall sensing
  best-effort sensor signals
  not a medical device

  7. Notifications wording

  Push notifications are needed for:

  check-in reminders
  Safety Circle alerts
  SOS/contact alerts
  incoming calls/messages
  Safe Walk/Safety Timer/Drive alerts

  Avoid implying guaranteed delivery.

  8. Contacts wording

  If contacts import exists, say:

  “StillHere uses contacts only when you choose someone for your Safety Circle.”

  Do not upload the full address book unless code actually does that.

  If full upload happens, flag it as high privacy risk.

  9. Microphone/calls wording

  If microphone is used for in-app calls:

  Say:

  “StillHere uses the microphone for calls you start or answer inside the app.”

  Avoid:

  monitoring audio
  listening in background

  10. Required report format

  Return:

  A. Current permission list found
  B. Missing permissions or unnecessary permissions
  C. Risk rating for each: high / medium / low
  D. Recommended replacement strings
  E. Required Xcode capabilities
  F. Android status
  G. App Store review notes
  H. Any copy that needs to align with privacy policy or onboarding limitations screen

  No code changes yet.

  After we agree on the wording and capability list, founder will approve implementation.

  Simple explanation:

  We’re asking Replit to inspect all app permission wording before Apple review.

  This matters because Apple checks whether permission prompts match what the app actually does.

  The biggest risk is background location. We need the wording to say:

  - location is off by default
  - location is used for safety features when enabled
  - StillHere is not tracking people all day by default
  - presence mode does not collect GPS during calm time
  - emergency flows may use location

  We also need to check HealthKit, Apple Watch, microphone, notifications, contacts, and Android status.